import { FilterPokemeonPipePipe } from './filter-pokemeon--pipe.pipe';

describe('FilterPokemeonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPokemeonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
